<% if movie.poster.attached? %>
 <%= image_tag movie.poster_url, alt: "#{movie.title} Poster", class: "img-thumbnail" if movie.poster_url.present? %>
<% elsif movie.poster_url.present? %>
  <%= image_tag movie.poster_url, alt: "#{movie.title} Poster", class: "img-thumbnail" %>
<% else %>
  <p>No poster available</p>
<% end %>
